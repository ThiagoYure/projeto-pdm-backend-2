import React from 'react';
import {Text, View, Button } from 'react-native';

const DetalheImovel  = ({ navigation }) => {
    return (
      <View>
        <Text>Detalhe de Imóvel</Text>
      </View>
    );
}

export default DetalheImovel;