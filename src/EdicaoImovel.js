import React from 'react';
import {Text, View, Button } from 'react-native';

const EdicaoImovel  = ({ navigation }) => {
    return (
      <View>
        <Text>Edição de Imóvel</Text>
      </View>
    );
}

export default EdicaoImovel;